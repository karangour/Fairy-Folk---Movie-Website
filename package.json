{
    "name": "fairy-folk---movie-website-root",
    "private": false,
    "version": "0.1.0",
    "scripts": {
      "build": "cd client && npm run build && cp -r client/dist/* .",
      "deploy": "npm run build && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin main"
    }
  }
  